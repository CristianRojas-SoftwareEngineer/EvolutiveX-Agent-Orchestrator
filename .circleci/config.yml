# CircleCI CI — tts-sidecar
# Job: Windows x86_64
version: 2.1

jobs:
  build-windows:
    machine:
      image: windows-server-2022-gui:current
    resource_class: windows.medium
    working_directory: ~/EvolutiveX-Agent-Orchestrator
    steps:
      - checkout
      - restore_cache:
          keys:
            - cargo-win-{{ checksum "sidecar/Cargo.lock" }}
            - cargo-win-
      - run:
          name: "Instalar Rust toolchain MinGW"
          command: |
            curl --proto '=https' --tlsv1.2 -sSf https://win.rustup.rs -o rustup-init.exe
            ./rustup-init.exe -y --default-toolchain stable
            export PATH="$USERPROFILE/.cargo/bin:$PATH"
            rustup set default-host x86_64-pc-windows-gnu
            rustup toolchain install stable-x86_64-pc-windows-gnu
            rustup target add x86_64-pc-windows-gnu --toolchain stable-x86_64-pc-windows-gnu
      - run:
          name: "Crear cargo config (GNU toolchain paths)"
          command: |
            set +e
            export PATH="$USERPROFILE/.cargo/bin:$PATH"
            mkdir -p diagnostics
            {
              echo "=== USERPROFILE ==="
              echo "$USERPROFILE"
              echo "=== LOCALAPPDATA ==="
              echo "$LOCALAPPDATA"
              echo "=== RUSTUP_HOME ==="
              echo "$RUSTUP_HOME"
              echo ""
              echo "=== rustup toolchain list ==="
              rustup toolchain list 2>&1
              echo ""
              echo "=== ls USERPROFILE/.rustup/toolchains ==="
              ls "$USERPROFILE/.rustup/toolchains/" 2>&1
              echo ""
              echo "=== ls toolchain GNU root ==="
              ls "$USERPROFILE/.rustup/toolchains/stable-x86_64-pc-windows-gnu/" 2>&1
              echo ""
              echo "=== ls toolchain GNU bin ==="
              ls "$USERPROFILE/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin/" 2>&1
              echo ""
              echo "=== rustup which gcc --toolchain stable-x86_64-pc-windows-gnu ==="
              rustup which gcc --toolchain stable-x86_64-pc-windows-gnu 2>&1
              echo ""
              echo "=== rustup which ar --toolchain stable-x86_64-pc-windows-gnu ==="
              rustup which ar --toolchain stable-x86_64-pc-windows-gnu 2>&1
            } > diagnostics/rustup-info.txt
            mkdir -p .cargo
            GCC_PATH="$USERPROFILE/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin/gcc.exe"
            AR_PATH="$USERPROFILE/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin/ar.exe"
            GCC_FW="${GCC_PATH//\\//}"
            AR_FW="${AR_PATH//\\//}"
            printf '[target.x86_64-pc-windows-gnu]\nar = "%s"\nlinker = "%s"\n' "$AR_FW" "$GCC_FW" > .cargo/config.toml
            {
              echo ""
              echo "=== .cargo/config.toml ==="
              cat .cargo/config.toml
            } >> diagnostics/rustup-info.txt
            cat diagnostics/rustup-info.txt
      - store_artifacts:
          path: diagnostics
          destination: diagnostics
      - run:
          name: "Compilar tts-sidecar"
          command: |
            set +e
            export PATH="$USERPROFILE/.cargo/bin:$PATH"
            GNU_TOOLCHAIN="$USERPROFILE/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin"
            export PATH="$GNU_TOOLCHAIN:$PATH"
            mkdir -p diagnostics
            {
              echo "gcc en PATH: $(which gcc 2>/dev/null || echo 'NO ENCONTRADO')"
              echo "gcc version:"
              gcc --version 2>&1 || echo "gcc falló"
            } > diagnostics/build-output.txt
            cargo build --release \
              --manifest-path sidecar/Cargo.toml \
              --target x86_64-pc-windows-gnu \
              2>&1 | tee -a diagnostics/build-output.txt
            echo "Exit code: $?" >> diagnostics/build-output.txt
      - store_artifacts:
          path: diagnostics/build-output.txt
          destination: diagnostics
      - save_cache:
          key: cargo-win-{{ checksum "sidecar/Cargo.lock" }}
          paths:
            - C:\Users\circleci\.cargo\registry
            - C:\Users\circleci\.cargo\git
      - run:
          name: "Empaquetar artefacto"
          command: |
            mkdir -p staging
            cp sidecar/target/x86_64-pc-windows-gnu/release/tts-sidecar.exe staging/
            cd staging && zip -r ../tts-sidecar-windows-x86_64.zip . && cd ..
            sha256sum tts-sidecar-windows-x86_64.zip | awk '{print "windows-x86_64=" $1}' \
              > tts-sidecar-windows-x86_64.sha256.txt
      - store_artifacts:
          path: tts-sidecar-windows-x86_64.zip
      - store_artifacts:
          path: tts-sidecar-windows-x86_64.sha256.txt

workflows:
  build-windows:
    jobs:
      - build-windows
