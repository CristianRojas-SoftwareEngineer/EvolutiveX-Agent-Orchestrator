<!-- BEHAVIORAL delta: use canonical operation headers (## ADDED/MODIFIED/REMOVED/RENAMED).
     A REMOVED/MODIFIED requirement (or a RENAMED FROM) MUST match a requirement that
     exists in openspec/specs/<capability>/spec.md.

     NON-CANONICAL delta: do NOT use operation headers. Replace this whole file with a
     non-canonical record (synchronize will not promote it to the canon). It documents
     items with no canonical counterpart — retirements OR additions (tests, tooling):

       ## Non-canonical record

       - `<item>` — <reason it has no canonical requirement> -->

## ADDED Requirements

### Requirement: <!-- requirement name -->
<!-- requirement text -->

#### Scenario: <!-- scenario name -->
- **WHEN** <!-- condition -->
- **THEN** <!-- expected outcome -->
