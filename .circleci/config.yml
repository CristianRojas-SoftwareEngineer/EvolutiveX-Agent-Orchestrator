# CircleCI CI — tts-sidecar
# Job: Windows x86_64
version: 2.1

jobs:
  build-windows:
    machine:
      image: windows-server-2022-gui:current
    resource_class: windows.medium
    working_directory: ~/EvolutiveX-Agent-Orchestrator
    steps:
      - checkout
      - restore_cache:
          keys:
            - cargo-win-{{ checksum "sidecar/Cargo.lock" }}
            - cargo-win-
      - run:
          name: "Instalar Rust toolchain MinGW"
          command: |
            curl --proto '=https' --tlsv1.2 -sSf https://win.rustup.rs -o rustup-init.exe
            ./rustup-init.exe -y --default-toolchain stable
            export PATH="$USERPROFILE/.cargo/bin:$PATH"
            rustup set default-host x86_64-pc-windows-gnu
            rustup toolchain install stable-x86_64-pc-windows-gnu
            rustup target add x86_64-pc-windows-gnu --toolchain stable-x86_64-pc-windows-gnu
      - run:
          name: "Crear cargo config con paths absolutos"
          command: |
            export PATH="$USERPROFILE/.cargo/bin:$PATH"
            mkdir -p .cargo
            # cc-rs y el linker de cargo usan estos paths absolutos
            GCC="$USERPROFILE/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin/gcc.exe"
            AR="$USERPROFILE/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin/ar.exe"
            cat > .cargo/config.toml \<< EOF
            [target.x86_64-pc-windows-gnu]
            ar = "C:/Users/circleci/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin/ar.exe"
            linker = "C:/Users/circleci/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin/gcc.exe"
            EOF
      - run:
          name: "Compilar tts-sidecar"
          command: |
            export PATH="$USERPROFILE/.cargo/bin:$PATH"
            # cc-rs busca gcc.exe en PATH para compilar build scripts de bzip2-sys y ring
            MINGW_DIR="$USERPROFILE/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin"
            export PATH="$MINGW_DIR:$PATH"
            cargo build --release \
              --manifest-path sidecar/Cargo.toml \
              --target x86_64-pc-windows-gnu
      - save_cache:
          key: cargo-win-{{ checksum "sidecar/Cargo.lock" }}
          paths:
            - C:\Users\circleci\.cargo\registry
            - C:\Users\circleci\.cargo\git
      - run:
          name: "Empaquetar artefacto"
          command: |
            mkdir -p staging
            cp sidecar/target/x86_64-pc-windows-gnu/release/tts-sidecar.exe staging/
            cd staging && zip -r ../tts-sidecar-windows-x86_64.zip . && cd ..
            sha256sum tts-sidecar-windows-x86_64.zip | awk '{print "windows-x86_64=" $1}' \
              > tts-sidecar-windows-x86_64.sha256.txt
      - store_artifacts:
          path: tts-sidecar-windows-x86_64.zip
      - store_artifacts:
          path: tts-sidecar-windows-x86_64.sha256.txt

workflows:
  build-windows:
    jobs:
      - build-windows